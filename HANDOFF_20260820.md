# Handoff prompt（接手上下文 @ 2026-08-20T13:55Z）

## 0. 硬约束（先读 AGENTS.md，以下为要点重述）
- 14 方法名册、路径、环境、checkpoint 只以 `formal_evaluation/config/baseline_runtime_registry_dsw.json` 为准；跑任何 baseline 前先 `python formal_evaluation/validate_runtime_registry.py --method <m> --strict`。
- 每次 run 必须用**全新唯一命名的输出目录**；不得覆盖/删除 checkpoint、formal 结果、他人产物与日志。删除前必须先确认：(a) 已有等效/更权威的副本或迁移目标，(b) 没有进程仍在引用该路径。
- GPU 运行前必须确认目标物理卡空闲并识别占用者；nvidia-smi 的 index 与 CUDA_VISIBLE_DEVICES 在部分节点不一致（5000 节点历史上出现过，6001 经实测一致），必要时用 `nvidia-smi --query-compute-apps` + `--query-gpu=index,uuid` 交叉核对。
- 未定义指标保持 NaN，不得置零。
- **暂停用 `kill -STOP`（保留一切已完成产物和可恢复状态），取消/清理用 `kill -9` + 明确核实后 `rm -rf`。两者不要混淆。**

## 1. 连接方式（macOS zsh 注意事项）
```
ssh -o StrictHostKeyChecking=no -o BatchMode=yes -i ~/.ssh/id_ed_pai -p {5000|5001|6001} root@39.106.218.186
```
- `~/.ssh/config` 只定义了 6001；5000/5001 需要显式 `-p` 和 `-i ~/.ssh/id_ed_pai`。
- zsh 不做变量分词：命令存到变量后要用 `${=VAR}` 调用，或直接内联。
- macOS 无 `timeout` 命令，改用 `-o ConnectTimeout`。
- **重要新发现**：三个节点（5000/5001/6001）彼此之间、甚至节点连自己的外部端口，都**连不通**（`Connection timed out`）。这三个 DSW pod 只对**你本机的出口 IP** 开放对应端口，pod 内部无法通过外部 IP 回环访问彼此或自己。任何需要"协调多节点"的自动化，只能跑在真正能同时连到三个端口的机器上——目前只有本机（用户的 Mac）满足这个条件。这也是本次会话把调度器部署在本机而不是任何 DSW pod 上的原因（见第 5 节）。
- nohup 加 `& disown` 的后台启动命令，本地 ssh 调用经常要卡 ~2 分钟才返回（哪怕远端任务其实几秒内就真正启动了）——这是这个环境的固定行为模式，不代表命令挂了；请给足超时（120–150s）而不是缩短超时。

## 2. 当前 CPFS 状态（⚠️ 曾经历 0 可用的空间危机）
- 本次会话进行到一半时 CPFS 从 385G 一路耗尽到 **0 可用（100% 已用）**，触发了一整套暂停动作（arctic 物化、两个 contact geometry cache 构建、本地调度器）。
- 之后把 h2o 的 contact geometry cache（436G）迁移验证完整后删除源数据，CPFS 回升到 **397G 可用**（接手时的最新读数，请自行 `df -h /mnt/workspace` 复核，此刻可能已经变化）。
- **20T 总量长期处于 95%+ 使用率是这套基础设施的常态**，任何新的大规模物化/评测前都应该先 `df -h /mnt/workspace` 确认，不要想当然。
- taco / hoi4d 两个数据集的物化**因为空间不足，一直没有启动**，这是本轮范围之外的已知缺口，恢复空间后再考虑。

## 3. OSSFS 迁移状态
- 挂载点：`/mnt/oss/research`、`/mnt/oss/pre-train`、`/mnt/oss/post-train`（各 512T，fuse.ossfs2）。
- **重要修复**：`formal_evaluation/promote_window_inputs_to_oss.py` 原本只存在于本地仓库、从未部署到远端；其 FUSE 活性检测读 `/sys/fs/fuse/connections`，这个路径在容器内**恒为空**（即使 ossfs2 真实可写），导致检测必然失败。已修复为真实的写入探测（写临时文件+读回+删除），已同步部署到远端 `/mnt/workspace/sjc/EgoFound3R-baselines_formal_7c79a98/formal_evaluation/promote_window_inputs_to_oss.py`。用这个工具前先确认远端有最新版本。
- **h2o contact geometry cache（436G）**：已完整迁移到 `/mnt/oss/research/eval_artifacts_promoted/h2o_contact_baseline/cache`（rsync -a，40个文件，大小/文件数与源端核对一致），**源数据已删除**（CPFS 因此释放 397G）。
- **h2o window_inputs（9.5G）**：迁移**可能仍在进行**（用的是 promote 工具的 `shutil.copytree` + 逐 json 重写路径，很多小文件，在 ossfs 上明显比 rsync 批量拷大文件慢很多——之前观察 26+ 分钟还没搞定 9.5G，而 436G 的 contact cache rsync 只用了 24 分钟）。目的地：`/mnt/oss/research/eval_artifacts_promoted/window_inputs/h2o`。**接手后第一件事**：检查这个进程是否还在跑（`ps -eo pid,cmd | grep promote_window_inputs`），跑完后核对 `promotion_h2o.json` 标记文件，确认无误后再决定是否删除 CPFS 源（`/mnt/workspace/sjc/eval_artifacts/prep_6dataset_60f_20260818T0410Z_8b1a806/window_inputs/h2o`，9.5G）。
- 其余数据集（hot3d/oakink_v2/arctic）的 window_inputs 和 contact cache **尚未迁移**，仍在 CPFS 上。

## 4. 各数据集当前进度（评测目标：min(strict manifest数, 400)；h2o=283 本身<400 无需封顶）

manifest 唯一权威版本：`prep_6dataset_60f_reduced_20260819T023430Z_d9d2963f/evaluation_test_windows_60f_strict.jsonl`（sha256=`1f993263029ee15d4d41363a1d5e910cba2c48fb8b3ce78226b09db9b8c234e4`，本地也有一份同哈希副本在 `formal_evaluation/datasets/manifests/`）。per-dataset 窗口数：h2o 283 / arctic 434 / hoi4d 461 / hot3d 1008 / oakink_v2 400 / taco 400。

### H2O（283/283 物化完成）
- **已完成（13个）**：egofound3r, wilor, hawor, pad_hand, reviv4d, s2contact, contactopt, vggt, pi3, da3_large_1_1, lingbot_map_long, vggt_omega（全部 283/283）+ **interactVLM（50 clips / 3000 帧，已确认完成，见上方订正）**。
- dyn_hamr：独立的 50-clip 子方案，50/50 已达标并自动暂停（看守脚本 `dyn_hamr_h2o_first50_pause_watcher_20260819T1710Z`）。
- **无未完成/未开始项**（interactVLM 和 dyn_hamr 严格说都不属于本轮"10方法"自动化范围，但两者在 h2o 上都已经用各自的小样本方案跑完了，等于提前完成了后续"更小窗口数"计划里 h2o 这一部分）。

### Hot3D（评测封顶400；物化 1008/1008 已完成）
| 方法 | 状态 |
|---|---|
| da3_large_1_1 | 1008 ✅完成 |
| wilor | 777 ✅完成（超额） |
| hawor | 669 ✅完成（超额） |
| egofound3r | 526 ✅完成（超额） |
| vggt_omega | 441 ✅完成（超额） |
| lingbot_map_long | 430 ✅完成（超额） |
| reviv4d | ~393/400，**运行中**（node 5000, PID 36901），接近达标 |
| pad_hand | ~122/400，**运行中**（node 5000 GPU0, PID 3209027），单窗口耗时异常长（早前一个shard耗时4.7小时），进度慢但确认存活 |
| vggt | 0，pending，等待调度器派发 |
| pi3 | 0，pending，等待调度器派发 |
| s2contact / contactopt | **本轮排除**，未启动（geometry cache 构建也已暂停，见第5节） |
| interactVLM | ✅ 已完成（50 clips/3000帧，见上方订正），进程留在暂停态未清理 |

### OakInk-v2（评测目标400；物化 400/400 已完成）
| 方法 | 状态 |
|---|---|
| hawor, vggt, pi3, da3_large_1_1, lingbot_map_long, vggt_omega | 均 400/400 ✅完成（进程会在耗尽400窗口后自然退出，无需外部暂停） |
| egofound3r | ~312/400，运行中（node 5000, PID 2379817） |
| wilor | ~264/400，运行中（node 5000, PID 2417096） |
| pad_hand | 0，pending |
| reviv4d | 0，pending |
| s2contact / contactopt | 本轮排除，未启动 |

### Arctic（物化目标434，评测封顶400；物化**未完成，进行中**）
- 物化进度：截至最近一次检查，2个完整 shard（100窗口）+ shard 2 部分完成（累计约36G），**已恢复**（此前因CPFS危机被 SIGSTOP，现已 SIGCONT）。root：`prep_6dataset_60f_strict_arctic_20260820T000131Z`。
- 尚未达到 400 窗口评测门槛，调度器暂不会派发任何方法（`dataset_ready()` 检查会自动识别，达标后自动纳入）。
- 已部署自动触发看守：`arctic_400_contact_cache_watcher_20260820T0415Z`，原计划在满400窗口后自动构建 contact cache——**但本轮 contact 方法整体排除在自动化外，这个看守目前仍然存活、逻辑仍会在满足条件时触发**，如果不想要这个行为，需要手动 kill 掉（PID 需要重新查一下，root目录里有）。

### Taco / HOI4D
物化未启动（CPFS 空间不足，一直搁置），无任何方法进度。

## 5. 自动调度器 —— `formal_evaluation/auto_scheduler.py`

### 部署位置与运行方式（⚠️ 重要）
**不在任何 DSW 节点上，跑在本机（用户的 Mac）本地后台**，原因见第1节（三个 pod 互相连不通，只有本机能同时连到三个端口协调调度）。

```bash
# 查看是否在跑
ps aux | grep auto_scheduler.py | grep -v grep

# 启动（如果没在跑）
cd /Users/jingchengshi/Desktop/MIMO-Rutgers/1-Codes/EgoFound3R-baselines
nohup python3 formal_evaluation/auto_scheduler.py > .auto_scheduler/stdout.log 2>&1 < /dev/null &
disown

# 停止
pkill -f "python3 formal_evaluation/auto_scheduler.py"
```

### 如何获取自动更新的进度（下一个 agent 应该怎么查）
- **实时状态**：`.auto_scheduler/state.json`（本机路径，仓库根目录下，git 未跟踪）——每个 `<dataset>::<method>` 的当前 `status`（done/running/pending/blocked_on_cache）、`count`（已完成窗口数）、`node`/`pid`（若在跑）。**这是唯一的权威实时进度源，不要凭记忆或旧的手动检查结果判断**，因为调度器每 5 分钟（`POLL_SECONDS=300`）会重新核对一轮并覆写这个文件。
- **事件日志**：`.auto_scheduler/scheduler.log`——记录每次 dispatch / pause 动作、进程存活探测的 warning、错误（`ERROR cycle failed: ...`）。daemon 本身用 `try/except` 包裹每一轮，单轮出错不会导致进程退出，但会记日志，值得定期看一眼有没有反复报错。
- **stdout（未捕获的致命错误才会出现在这里）**：`.auto_scheduler/stdout.log`——正常情况下应该是空的；如果这里有内容说明整个 daemon 崩溃退出了，需要看 traceback 排查再重启（**不要不看原因就直接重启**，之前有一次崩溃是因为 dispatch 的 ssh 超时设太短（20s），已经修复为150s + try/except 包裹，但如果又出现新的崩溃，说明还有没覆盖到的边界情况）。

### 本轮调度范围（2026-08-20 最新版）
- 数据集：h2o / hot3d / oakink_v2 / arctic（arctic 要等物化满400窗口才会被纳入，见 `dataset_ready()`）。
- 方法：**只有 10 个标准方法**（egofound3r, wilor, hawor, pad_hand, reviv4d, vggt, pi3, da3_large_1_1, lingbot_map_long, vggt_omega）。**s2contact、contactopt、interactVLM、dyn_hamr 全部排除在外**，不是"阻塞等前置条件"，是本轮完全不碰。
- 400窗口封顶：达标（`count >= target`）即对该 job 发 `SIGSTOP`（含所有子进程），保留已产出内容，不删减超跑部分。
- GPU 探测：`nvidia-smi` 查询 `utilization<=5% and free_mb>=15000` 判定为可用，5000节点GPU 2/3/4 被硬编码排除（长期被不明第三方进程占用，`NODE_ALLOWED_GPUS` 里写死）。
- 进程存活判定：**不要用 `pgrep -f <job专属子串>` 直接在远端跑**——这是踩过的坑：pgrep 的搜索模式文本会出现在包装它的 ssh/bash 调用自身的命令行里，导致自我匹配，返回一个不断变化的、实际已经退出的伪 PID。现在的做法是每轮先 `ps -eo pid,ppid,cmd` 拉全量进程表（3个节点各一次），再在本地内存里字符串匹配，**这是唯一验证有效的方式，不要改回远端 pgrep -f**。

### 已知需要人工决策的点
- `hot3d::reviv4d` 和 `hot3d::pad_hand` 目前还没触达400，仍在正常推进，等它们各自达标，调度器会自动暂停并腾出GPU接着派发下一个 pending 任务（`hot3d::vggt`、`hot3d::pi3`、`oakink_v2::pad_hand`、`oakink_v2::reviv4d` 目前排在队列里）。
- arctic 一旦满足400窗口，调度器会自动把它的 10 个标准方法全部加入候选池，会显著增加并发调度压力，接手后建议留意首次 arctic 派发时的 GPU 争抢情况。

## 6. 本轮排除、留到以后处理的方法
- **s2contact / contactopt**：h2o 已完整跑完（283/283）。hot3d 和 oakink_v2 的 geometry cache 构建过程做到一半被 CPFS 危机打断，**已 SIGSTOP 暂停，未恢复**（对应4个进程，具体 PID 需要重新用 `ps -eo pid,stat,cmd | grep build_s2_contactopt_geometry_cache` 查，之前记录的 PID 在长时间运行后可能已变化或被系统回收）。恢复方式：`kill -CONT <pid>`。工具已固化：`formal_evaluation/contact/adapters/build_contact_geometry_cache_for_dataset.sh`，用法见脚本头注释。
- **interactVLM / dyn_hamr**：按计划要"等其余方法做完，再单独给一个更小窗口数的方案"——但如上所述，**h2o 这一部分实际上已经提前用 50-clip 方案做完了**（interactVLM 3000帧、dyn_hamr 50 clips），后续只需要补 hot3d/oakink_v2/arctic 等数据集的小样本版本即可，不用重新规划 h2o。

## 7. 本次会话修复/新增的代码（已提交到本地仓库，需要 git 状态自查是否已提交）
- `formal_evaluation/auto_scheduler.py`（新增）——本机运行的调度器，见第5节。
- `formal_evaluation/promote_window_inputs_to_oss.py`（新增本地文件+修复+部署到远端）——OSSFS 迁移工具，FUSE 活性检测已修复为写入探测。
- `formal_evaluation/contact/adapters/build_contact_geometry_cache_for_dataset.sh`（新增）——contact geometry cache 构建的固化脚本，供 hoi4d/taco 未来物化后直接复用。
- `formal_evaluation/datasets/manifests/evaluation_test_windows_60f_strict_20260819T023430Z_d9d2963f.jsonl`（此前会话已同步，本次确认哈希一致）。

## 8. 复用的排查手法（沿用自上一轮 handoff，依然有效）
- 统计方法进度：`find <root>/<method> -name run.json | xargs grep -l '"status": "success"' | wc -l`；不要依赖 queue-root 的 done/pending 目录（可能已被清空或从未使用）。
- **有些方法用的不是 run_formal_window_queue.py 那套 schema**（比如 interactVLM 是独立 JSONL pipeline，本次就因为没意识到这点漏判了两个已完成任务），排查一个方法进度前，先确认它到底是哪种输出结构，不要想当然套用标准 schema 去 find/grep。
- 判断任务真实所在节点：CPFS 共享导致文件到处可见，必须 `ps -eo pid,ppid,stat,cmd` 在各节点分别确认进程；STAT=T 表示已被 SIGSTOP。
- GPU 归属判断：优先看进程的 `--gpus N` / `CUDA_VISIBLE_DEVICES` 参数，其次用 `nvidia-smi --query-compute-apps` 配合 uuid→index 映射交叉核对；不要只看显存/利用率数字下结论。
